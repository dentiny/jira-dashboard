{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PR Rework Assessment",
  "description": "Output format for the PR tasks stage. The coder lists which checks need code changes (rework) vs which were resolved via non-code actions.",
  "type": "object",
  "properties": {
    "rework_checks": {
      "type": "array",
      "description": "Checks that require code changes to resolve. These will trigger an auto-move to clarification for a separate code rework flow.",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Full check name as shown in the PR status checks (e.g. 'Code Checks', 'Simtest Status')"
          },
          "reason": {
            "type": "string",
            "description": "Why this check requires code changes to resolve"
          }
        },
        "required": ["name", "reason"]
      }
    },
    "touched_checks": {
      "type": "array",
      "description": "Checks the coder took action on that do not require code changes (e.g. re-triggered a CI job, added a label, commented).",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Full check name as shown in the PR status checks"
          },
          "action": {
            "type": "string",
            "description": "What action the coder took (e.g. 're-triggered CI job', 'added label', 'commented on PR')"
          },
          "result": {
            "type": "string",
            "description": "What happened as a result (e.g. 'job launched', 'label added', 'comment posted')"
          }
        },
        "required": ["name", "action", "result"]
      }
    },
    "resolved_comments": {
      "type": "array",
      "description": "Open review comments that were resolved without code changes. The coder closes each thread (optionally with a reply) and logs it here.",
      "items": {
        "type": "object",
        "properties": {
          "comment_id": {
            "type": "number",
            "description": "GitHub comment databaseId of the resolved review comment"
          },
          "reply": {
            "type": "string",
            "description": "Optional reply posted before resolving the thread"
          }
        },
        "required": ["comment_id"]
      }
    }
  },
  "required": ["rework_checks", "touched_checks"]
}
